import React from 'react';
import Box from "@mui/material/Box";

interface RowProps {
  isSelected: boolean;
}
const Row: React.FC<RowProps> = (props) => {
  return (
    <Box></Box>
  );
}

export default Row;